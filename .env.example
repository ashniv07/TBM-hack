# Postgres (structured data: datasets, columns, relationships, quality, ATUM mappings)
DATABASE_URL=postgres://postgres:postgres@localhost:55432/tbm_trust
# Optional: enables LLM-based source-type classification and (later) semantic
# understanding / RAG stages. Without it, Stage 2 falls back to keyword heuristics.
OPENAI_API_KEY=

# API server port
PORT=4000
